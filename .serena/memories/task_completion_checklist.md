Before finishing changes: restore/build affected projects; run relevant tests (at least targeted tests, full Test/Test.csproj when feasible); run dotnet format --verify-no-changes for formatting drift; confirm no unintended generated artifacts are left behind (especially *_Entrapment.xml/temp files); if CLI options changed update README option table; ensure mimic.exe and TSV assets still have CopyToOutputDirectory metadata; summarize commands executed and outcomes for handoff.